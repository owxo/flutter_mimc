/// Optional features exposed by an individual MIMC SDK implementation.
enum MimcCapability {
  message,
  groupMessage,
  onlineMessage,
  unlimitedGroup,
  offlinePull,
  realtimeStream,
  realtimeChannel,
}
