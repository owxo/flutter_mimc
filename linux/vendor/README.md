Place `libmimc_sdk.so` and its non-system shared-library dependencies here.
Flutter's Linux build will copy every `*.so*` file into the application bundle.
Run `tool/build_desktop_sdk.sh linux` from the repository root to build it.
