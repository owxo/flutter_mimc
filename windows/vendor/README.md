Place `mimc_sdk.dll` and its non-system DLL dependencies here. Flutter's
Windows build will copy every DLL in this directory into the application.
From Git Bash with `VCPKG_ROOT` set, run
`tool/build_desktop_sdk.sh windows` to build and collect them.
